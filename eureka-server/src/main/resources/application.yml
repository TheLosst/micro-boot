server:
  port: 8761

spring:
  application:
    name: eureka-server
  banner:
    location: banner.txt
  eureka:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
