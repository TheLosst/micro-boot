server:
  port: 8082

spring:
  application:
    name: account-service
#  datasource:
#    url: jdbc:postgresql://localhost:5432/accounts_db
#    username: postgres
#    password: 1234
#    driver-class-name: org.postgresql.Driver

  datasource:
    url: jdbc:postgresql://51.250.26.59:5432/pc_db
    username: secUREusER
    password: StrongEnoughPassword)
    driver-class-name: org.postgresql.Driver

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/